const Todos = require('./models');

module.exports = {

  // findAll: () => Todos.find({}),
  // createOne: (todo, listName) => Todos.create({ todo, listName }),
  // deleteAll: () => Todos.deleteMany({}),
  // findOne: () => Todos.
  // updateOne: (todo, _id) => Todos.updateOne({ _id }, { todo }),

}